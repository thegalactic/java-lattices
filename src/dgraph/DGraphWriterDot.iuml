package dgraph {
    class DGraphWriterDot {
    	-{static}DGraphWriterDot instance
        +void write(DGraph graph, DataOutputStream out)
        +{static}DGraphWriterDot getInstance()
        +{static}void register()
    }

    DGraphWriter <|-- DGraphWriterDot
}
